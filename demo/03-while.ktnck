"Iterating 1 - 6, checking for Odd and Even" P
1 while dup 7 < do
    "Index: " p dup .
    dup 2 % 0 = if
        "Even" P
    end
    dup 2 % 1 = if
        "Odd" P
    end
    1 +
end
drop
"End of test!" P